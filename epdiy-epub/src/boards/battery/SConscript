Import('RTT_ROOT')
Import('rtconfig')
from building import *

cwd = GetCurrentDir()
src = Glob('*.cpp')
CPPPATH = [cwd]

group = DefineGroup('battery', src, depend = [''],CPPPATH = CPPPATH)

Return('group')
